# Recursion Practice

A couple of recursion examples. First example computes the factorial for a given number. 
The second example calculates A Fibonacci Number sequence by first asking the user for the 
first two numbers in the sequence, and then asking the user which position in the Fibonacci 
sequence they would like to return a value for.
